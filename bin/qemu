#!/bin/sh

if [ "$1" = "m" ]; then
	# stdio is mini uart
	qemu-system-aarch64 -M raspi3 -kernel kernel8.img -display none -serial null -serial stdio
else 
	# stdio is pl011 uart
	qemu-system-aarch64 -M raspi3 -kernel kernel8.img -display none -serial stdio -serial null
fi
